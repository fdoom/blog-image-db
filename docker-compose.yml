services:
  image-api:
    build: ./image-api
    volumes:
      - ./image-api/src:/app/src  # 코드만 마운트
    environment:
      - TZ=Asia/Seoul
    ports:
      - "8888:8080"
    depends_on:
      - db
  db:
    image: mariadb:10.5
    ports:
      - 3306:3306
    volumes:
      - ./db/conf.d:/etc/mysql/conf.d
      - ./db/data:/var/lib/mysql
    env_file: .env
    environment:
      TZ: Asia/Seoul
    restart: always